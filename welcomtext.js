const welcome = (number, groupname) => {
    return `Hola @${number}. Bienvenido al grupo ${groupname} lee las reglas para no tener mal entendidos para uso del bot coloque el comando !menu`
}
exports.welcome = welcome

const bye = (number) => {
    return `Adios joto xd @${number}. 👋`
}
exports.bye = bye
